{
  "name": "@ai-western-town/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "pnpm --filter @ai-western-town/ui-sdk build && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit && vite build",
    "typecheck": "pnpm --filter @ai-western-town/ui-sdk build && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "preview": "vite preview",
    "test": "node --import tsx --test src/**/*.test.ts src/**/*.test.tsx"
  },
  "dependencies": {
    "@ai-western-town/contracts": "workspace:*",
    "@ai-western-town/ui-sdk": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.0",
    "tsx": "^4.20.6",
    "vite": "^7.1.12"
  }
}
