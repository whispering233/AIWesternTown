services:
  seq:
    image: datalust/seq:latest
    container_name: ai-western-town-seq
    environment:
      ACCEPT_EULA: "Y"
    ports:
      - "5341:80"
    volumes:
      - ./.seq:/data
    restart: unless-stopped
